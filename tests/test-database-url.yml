name: Test database-url input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create migration file
        run: |
          mkdir -p /tmp/migrations
          cat > /tmp/migrations/001_create_users.sql << 'EOF'
          CREATE TABLE users (
            id SERIAL PRIMARY KEY,
            email VARCHAR(255) NOT NULL
          );
          EOF

      - id: action
        uses: .
        continue-on-error: true
        with:
          migration-path: /tmp/migrations
          database-url: 'postgresql://postgres:postgres@localhost:5432/testdb'
          fail-on: 'never'

      - name: Assert action ran with database-url input
        env:
          OUTCOME: ${{ steps.action.outcome }}
          RISK: ${{ steps.action.outputs.risk-level }}
          VIOLATIONS: ${{ steps.action.outputs.violations }}
        run: |
          echo "outcome: $OUTCOME"
          echo "risk-level: $RISK"
          echo "violations: $VIOLATIONS"
          if [ "$OUTCOME" = "success" ]; then
            if [ -z "$RISK" ]; then
              echo "FAIL: action succeeded but risk-level output is empty"
              exit 1
            fi
            echo "PASS: action succeeded with risk-level=$RISK violations=$VIOLATIONS"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (no live DB available, which is expected in CI)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
